import React from "react"
import styles from "./index.module.scss"

export type IProps = {

}

export const __component__(pascalCase): React.FC<IProps> = (props: IProps) => {
    return (
        <div>Hello World</div>
    )
}
