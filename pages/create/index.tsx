import React from "react"
import { Text, LoginForm, SplitScreen, Link, CreateForm } from "@components"
import { signInInputs, signInInputsSchema } from "../../data/form"

const Create = () => { 
  return (
    <CreateForm/>
  )
}

export default Create
